import { printWgslTokens } from "./token-printer.js";
import { RenameAllocator } from "./rename-allocator.js";
import { analyzeWgslTokens } from "./scope-walker.js";
import { scan } from "./scanner.js";
const localKinds = new Set(["param", "let", "var", "const"]);
export function minifyWgslIdentifiers(source) {
    return applyIdentifierMinifyWgsl(source).wgsl;
}
export function applyIdentifierMinifyWgsl(source, options = {}) {
    const tokens = scan(source);
    const analysis = analyzeWgslTokens(tokens);
    const replacements = buildIdentifierReplacements(analysis);
    return {
        wgsl: options.whitespace === false ? applyReplacementsPreservingTrivia(source, tokens, replacements) : printWgslTokens(tokens, { replacements }),
        replacements,
        skippedHelperFunctions: skippedHelpers(analysis, replacements),
        fallbackReasons: collectFallbackReasons(analysis),
    };
}
function applyReplacementsPreservingTrivia(source, tokens, replacements) {
    if (replacements.size === 0)
        return source;
    let out = "";
    let offset = 0;
    for (let i = 0; i < tokens.length; i++) {
        const replacement = replacements.get(i);
        if (replacement === undefined)
            continue;
        const token = tokens[i];
        out += source.slice(offset, token.start);
        out += replacement;
        offset = token.end;
    }
    out += source.slice(offset);
    return out;
}
export function buildIdentifierReplacements(analysis) {
    const replacements = new Map();
    if (analysis.fallback.wholeModule)
        return replacements;
    const fileScopeNames = collectFileScopeNames(analysis);
    renameHelperFunctions(analysis, fileScopeNames, replacements);
    renameFunctionLocals(analysis, fileScopeNames, replacements);
    return replacements;
}
function renameHelperFunctions(analysis, fileScopeNames, replacements) {
    const helperDecls = analysis.declarations
        .filter((decl) => decl.kind === "function" && decl.safeToRename)
        .sort((a, b) => a.tokenIndex - b.tokenIndex);
    if (helperDecls.length === 0)
        return;
    const accounted = new Set();
    for (const decl of helperDecls) {
        accounted.add(decl.tokenIndex);
        for (const ref of analysis.references)
            if (ref.declarationId === decl.id)
                accounted.add(ref.tokenIndex);
    }
    const reserved = allIdentifierTextsExcept(analysis.tokens, accounted);
    for (const name of fileScopeNames)
        reserved.add(name);
    const allocator = new RenameAllocator({ reserved });
    for (const decl of helperDecls) {
        if (!allOccurrencesAccountedFor(analysis, decl, accounted))
            continue;
        const name = allocator.allocate();
        replacements.set(decl.tokenIndex, name);
        for (const ref of analysis.references)
            if (ref.declarationId === decl.id)
                replacements.set(ref.tokenIndex, name);
        fileScopeNames.add(name);
    }
}
function renameFunctionLocals(analysis, fileScopeNames, replacements) {
    const skippedFunctionIds = new Set(analysis.functions.filter((fn) => fn.skipped).map((fn) => fn.id));
    for (const fn of [...analysis.functions].sort((a, b) => a.id - b.id)) {
        if (skippedFunctionIds.has(fn.id))
            continue;
        const declarations = analysis.declarations
            .filter((decl) => decl.functionId === fn.id && decl.safeToRename && localKinds.has(decl.kind))
            .sort((a, b) => a.tokenIndex - b.tokenIndex);
        if (declarations.length === 0)
            continue;
        const replaceable = new Set();
        for (const decl of declarations) {
            replaceable.add(decl.tokenIndex);
            for (const ref of analysis.references)
                if (ref.declarationId === decl.id)
                    replaceable.add(ref.tokenIndex);
        }
        const reserved = new Set(fileScopeNames);
        for (const name of inFunctionUnrenamedIdentifierTexts(analysis.tokens, fn, replaceable))
            reserved.add(name);
        const allocator = new RenameAllocator({ reserved });
        for (const decl of declarations) {
            const name = allocator.allocate();
            replacements.set(decl.tokenIndex, name);
            for (const ref of analysis.references)
                if (ref.declarationId === decl.id)
                    replacements.set(ref.tokenIndex, name);
        }
    }
}
function collectFileScopeNames(analysis) {
    const names = new Set();
    for (const decl of analysis.declarations)
        if (decl.functionId === undefined)
            names.add(decl.name);
    for (const item of analysis.preservedTokens) {
        if (item.reason === "global" || item.reason === "struct") {
            const token = analysis.tokens[item.tokenIndex];
            if (token?.kind === "ident")
                names.add(token.text);
        }
    }
    return names;
}
function allIdentifierTextsExcept(tokens, except) {
    const names = new Set();
    for (let i = 0; i < tokens.length; i++) {
        const token = tokens[i];
        if (token.kind === "ident" && !except.has(i))
            names.add(token.text);
    }
    return names;
}
function inFunctionUnrenamedIdentifierTexts(tokens, fn, replaceable) {
    const names = new Set();
    for (let i = fn.nameTokenIndex; i <= fn.bodyEndToken; i++) {
        const token = tokens[i];
        if (token?.kind === "ident" && !replaceable.has(i))
            names.add(token.text);
    }
    return names;
}
function allOccurrencesAccountedFor(analysis, decl, accounted) {
    for (let i = 0; i < analysis.tokens.length; i++) {
        const token = analysis.tokens[i];
        if (token.kind === "ident" && token.text === decl.name && !accounted.has(i))
            return false;
    }
    return true;
}
function skippedHelpers(analysis, replacements) {
    return analysis.declarations
        .filter((decl) => decl.kind === "function" && decl.safeToRename && !replacements.has(decl.tokenIndex))
        .map((decl) => decl.name);
}
function collectFallbackReasons(analysis) {
    return [
        ...analysis.fallback.reasons,
        ...analysis.functions.flatMap((fn) => fn.fallbackReasons),
    ];
}
//# sourceMappingURL=identifier-minify.js.map